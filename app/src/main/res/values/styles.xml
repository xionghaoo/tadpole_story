<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomSeekbarStyle" >
        <item name="android:maxHeight">@dimen/_10dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@color/colorAccent</item>
        <item name="android:progressDrawable">@drawable/seekbar_progress_drawable</item>
        <item name="android:minHeight">@dimen/_10dp</item>
        <item name="android:thumb">@drawable/seekbar_slider_drawable</item>
    </style>

    <style name="pop_animation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_in</item>
        <item name="android:windowExitAnimation">@anim/pop_out</item>
    </style>

    <style name="RotateAnim" parent="android:Animation">
        <item name="android:animation">@anim/rotate</item>
    </style>
</resources>